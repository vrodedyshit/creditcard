Program supports custom flags as:
    - validate (validates given credit card numbers using Luhn's algorithm)
    - generate (generates up to 4 last digits of a credit card with a given prefix)
    - information (reads from files brands.txt and issuers.txt and provides a cars's brand and issuer)
    - issue (generates a random credit card number of a given brand and issuer)
